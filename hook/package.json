{
  "name": "@popcorn/hook",
  "version": "0.1.0",
  "private": true,
  "description": "Claude Code hook for file watching, test plan generation, and HTTP bridge",
  "license": "MIT",
  "type": "module",
  "main": "./src/index.ts",
  "bin": {
    "popcorn": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node --loader ts-node/esm src/index.ts",
    "test": "vitest run"
  },
  "dependencies": {
    "chokidar": "^3.6.0"
  },
  "devDependencies": {
    "ts-node": "^10.9.0"
  }
}
